package com.kitewatch.app.navigation

import androidx.compose.runtime.Composable

// Replaced by KiteWatchNavHost.kt — kept as empty file (DummyNavHost())
// to avoid breaking any future references to this package
// from AndroidManifest or build scripts.

@Suppress("EmptyFunctionBlock")
@Composable
fun DummyNavHost() {}
